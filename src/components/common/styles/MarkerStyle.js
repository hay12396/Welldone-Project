const K_WIDTH = 20;
const K_HEIGHT = 20;

export default {
    position: 'absolute',
    width: K_WIDTH / 2,
    height: K_HEIGHT / 2,
    left: -K_WIDTH / 2,
    top: -K_HEIGHT / 2,

    border: '1px solid white',
    borderRadius: K_HEIGHT,
    backgroundColor: 'red',
    textAlign: 'center',
    color: '#black',
    fontSize: 16,
    fontWeight: 'bold',
    padding: 4
};